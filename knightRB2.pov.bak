#include "socleRB.pov"

#macro Knight()
    Socle(Black)
    
    #local A = <0,0,0.8>;
    #local B = <0,-0.5,5>;

    sphere {
        <0,0,0>, 1.2 // <x, y, z>, radius
        scale<1,1,3.8>
        rotate<-10,0,0>
        translate A
        pigment{color Red}
    }
 
    sphere {
    <0,0,0>, 0.8 // <x, y, z>, radius
    scale<1,2,1>
    rotate<20,0,0>
    translate B
    pigment{color Cyan}
    }

    cone {
        <0.3,0,5.5>, 0.2 ,<0.5,0,5.9>,0.0
        pigment{color Yellow}
    }




#end

